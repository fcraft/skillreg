# ui-framework (placeholder submodule)

UI style framework for the skillreg dashboard (the `Q*`-prefixed component
family + `ThemeSwitch` + `NavItem` + `QLayout`, per PRD §5.3).

> **Status:** placeholder for v1. In v1 the existing `Q` components are copied
> verbatim into the dashboard and used as-is. Generalization / multi-brand
> diversity — and extraction into this submodule — is a **v1.x** scope item
> (PRD §5.2). This directory exists only to reserve the monorepo layout — no
> `git submodule add` has been run yet.
