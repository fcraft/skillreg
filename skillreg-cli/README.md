# skillreg-cli (placeholder submodule)

Companion CLI for skillreg, distributed as a Python package via `uv`.

> **Status:** placeholder. The real `skillreg-cli` submodule is wired up in a
> later issue (see `specs/skillreg-v2.md` §2.1 / §2.5). This directory exists
> only to reserve the monorepo layout — no `git submodule add` has been run yet.
